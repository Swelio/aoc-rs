use aoc_core::{components::TextInput, ports::Parser};

use super::Input;

impl Parser<&TextInput> for Input {
    fn try_parse(input: &TextInput) -> aoc_core::ParsingResult<Self> {
        todo!()
    }
}
